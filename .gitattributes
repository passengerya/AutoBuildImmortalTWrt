# 构建脚本在 Linux 容器内执行，YAML 被 GitHub Actions 解析，必须保持 LF 行尾
*.sh     text eol=lf
*.yml    text eol=lf
*.yaml   text eol=lf
*.conf   text eol=lf
*.md     text eol=lf
*.txt    text eol=lf
# run 自解压包 / ipk 是二进制载荷，禁止任何行尾转换，防止损坏
*.run    binary
*.ipk    binary
